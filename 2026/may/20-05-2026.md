---

### E-Sign Orders

**`lawyer_esign_orders.php` + `api/dashboard/orders.php`**

- Root cause: API queried `type='Lawyer'` on `tmp_estamp` — the actual column is `type_of_user` with value `'lawyer'` (lowercase). Zero rows returned.
- Added signed-doc check, stat cards, View/Invoice/Continue/Signed Doc action buttons, document title as clickable link.

**`lawyer-esign-order-details.php`** (new)

- Full detail page: order info, payment summary, parties grid with signing progress bar, delivery addresses, signed doc download, send-to-parties AJAX, cancellation form with eligibility check.
- Cannot use `get_estamp_order()` (reads `$_SESSION['sess_lawyer']`) — used direct PDO with ownership verification instead.
- Fixed `spo.vendor_id` SQL error — `vendor_id` lives in `tmp_estamp`, not `stamp_paper_orders`.

---

### E-Court Fee Orders

**`lawyer-ecourt-self.php` + `lawyer-ecourt-client.php`**

- Root cause: API queried `tbl_ecourt` (legacy); data lives in `e_court_fee_orders`. Also `order_type` values are `'For Self'` / `'For Client'` (not `'Self'` / `'Client'`). Found by direct DB query.
- Fixed all column mappings: `order_date`, `payment_status`, `payable_amount`, etc.
- Added stat cards, state name (JOIN), order number, quantity, View/Invoice buttons.

**`lawyer-ecourt-order-details.php`** (new)

- Two-column detail page: order details, client details (client orders), billing breakdown, transaction IDs, delivery address, vendor status.
- Links to existing `invoice/ecourt-fee-invoice.php` for the printable invoice.
- `@media print` hides sidebar/nav for clean print.

---

### Articles & Q&A

**`lawyer-articles.php`** — fixed 3 wrong column names: `reg_date`→`date`, `approved`→`publish`, no `status` column. Status badge now shows Draft / Published / Pending Review correctly.

**`edit-article.php`** (new) — Quill.js rich text editor, category dropdown (92 options), image upload with live preview, Save as Draft / Submit for Review, Delete with confirm dialog.

**`api/dashboard/articles.php`** (new) — `update` (ownership-verified, MIME-checked image upload, strip unsafe HTML tags), `delete`, `toggle_draft`.

**`lawyer-qna.php`** — complete rewrite: compact table with Q subject + answer preview (160 chars), inline expand-to-edit with Save/Cancel, delete with fade-out animation, windowed pagination.

**`api/dashboard/qna.php`** (new) — `update_answer` / `delete_answer`, both ownership-verified.

---

### Wallet

**`api/dashboard/wallet.php`**

| Bug | Fix |
| --- | --- |
| `transactions` queried `tbl_wallet` (5-col legacy) | Now queries `wallet_payments` (full ledger) |
| `summary` pending used `tbl_withdrawl` | Now uses `withdrawals WHERE request_status=0` |
| `withdrawals` read from `tbl_withdrawl` | Now reads from `withdrawals` table |
| `request_withdrawal` inserted into `tbl_withdrawl` | Now inserts into `withdrawals` with `balance_at_request` |
| No filters | Added `tx_type`, `date_from`, `date_to` |

**`lawyer-wallet.php`** — transactions now show description, credit/debit badge, balance after; filter bar (type + date range); withdrawal table shows bank ref, notes, approval date; balance discrepancy audit alert.

**Balance reconciliation** — found ₹83,026.81 gap (balance jumped from ~₹82,373 to ₹1 between two transactions with no debit record — direct phpMyAdmin modification). Inserted a single debit entry (`wallet_payments id=128`, `EAREC20260520...`) to make computed balance = stored balance = ₹10,002 ✓.

---

### 5 Bug Fixes (final batch)

| Issue | Fix |
| --- | --- |
| `lawyer-reviews.php` — no data | Table is `reviews`, not `tbl_review`. Added dual join (`tbl_user` + `tbl_lawyer`) with `COALESCE`, reply display |
| Case requests location showing "1, 1" | Added `LEFT JOIN tbl_state` + `LEFT JOIN tbl_city` to API; JS reads `state_name`/`city_name` |
| `lawyer-case-request-view.php` — old Bootstrap 3 design | Full rewrite: new layout, URL decoded (`cr_` prefix stripped), claim form with AJAX, client details locked until claimed |
| `edit-profile-lawyer#practice` — checkbox grid, broken courts loader | Replaced with **Select2.js** searchable multi-select for Practice Areas, Courts, Languages; courts loaded from `tbl_court` via profile API |
| `edit-profile-lawyer#personal` — wrong designation table | `tbl_designation1` (DCM2 admin roles) → `tbl_designation` (legal firm designations: Managing Partner, Partner, etc.) |

---

### Files Created/Modified Today

**New files:** `lawyer-esign-order-details.php`, `lawyer-ecourt-order-details.php`, `edit-article.php`, `api/dashboard/articles.php`, `api/dashboard/qna.php`, `lawyer-case-request-view.php`

**Modified:** `lawyer_esign_orders.php`, `lawyer-ecourt-self.php`, `lawyer-ecourt-client.php`, `lawyer-qna.php`, `lawyer-reviews.php`, `lawyer-wallet.php`, `lawyer-articles.php`, `lawyer-case-requests.php`, `edit-profile-lawyer.php`, `api/dashboard/orders.php`, `api/dashboard/wallet.php`, `api/dashboard/profile.php`, `api/dashboard/case-requests.php`, `api/dashboard/qna.php`